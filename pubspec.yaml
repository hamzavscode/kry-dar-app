name: mon_projet
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^4.9.0
  firebase_auth: ^6.5.1
  cloud_firestore: ^6.4.1
  image_picker: ^1.1.2
  http: ^1.2.2
  geolocator: ^14.0.2
  geocoding: ^4.0.0
  intl: ^0.20.2



dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^6.0.0


flutter:
  uses-material-design: true
  assets:
    - assets/images/arch_background.png   # → Image 1 (arch + lantern)
    - assets/images/moroccan_door.png     # → Image 2 (door with plants)
    - assets/images/plants.png     # → Image 3 (plants only)
